# Other Classes (732)

- AESContext <- RefCounted — Provides access to AES encryption/decryption of raw data.
- AStar2D <- RefCounted — An implementation of A* for finding the shortest path between two vertices on a connected graph in 2D space.
- AStar3D <- RefCounted — An implementation of A* for finding the shortest path between two vertices on a connected graph in 3D space.
- AStarGrid2D <- RefCounted — An implementation of A* for finding the shortest path between two points on a partial 2D grid.
- AcceptDialog <- Window — A base dialog used for user notification.
- AimModifier3D <- BoneConstraint3D — The AimModifier3D rotates a bone to look at a reference bone.
- AnimatedTexture <- Texture2D — Proxy texture for simple frame-based animations.
- Animation <- Resource — Holds data that can be used to animate anything in the engine.
- AnimationLibrary <- Resource — Container for Animation resources.
- AnimationMixer <- Node — Base class for AnimationPlayer and AnimationTree.
- AnimationNode <- Resource — Base class for AnimationTree nodes.
- AnimationNodeAdd2 <- AnimationNodeSync — Blends two animations additively inside of an AnimationNodeBlendTree.
- AnimationNodeAdd3 <- AnimationNodeSync — Blends two of three animations additively inside of an AnimationNodeBlendTree.
- AnimationNodeAnimation <- AnimationRootNode — An input animation for an AnimationNodeBlendTree.
- AnimationNodeBlend2 <- AnimationNodeSync — Blends two animations linearly inside of an AnimationNodeBlendTree.
- AnimationNodeBlend3 <- AnimationNodeSync — Blends two of three animations linearly inside of an AnimationNodeBlendTree.
- AnimationNodeBlendSpace1D <- AnimationRootNode — A set of AnimationRootNodes placed on a virtual axis, crossfading between the two adjacent ones.
- AnimationNodeBlendSpace2D <- AnimationRootNode — A set of AnimationRootNodes placed on 2D coordinates, crossfading between the three adjacent ones.
- AnimationNodeBlendTree <- AnimationRootNode — A sub-tree of many type AnimationNodes used for complex animations.
- AnimationNodeExtension <- AnimationNode — Base class for extending AnimationRootNodes from GDScript, C#, or C++.
- AnimationNodeOneShot <- AnimationNodeSync — Plays an animation once in an AnimationNodeBlendTree.
- AnimationNodeOutput <- AnimationNode — The animation output node of an AnimationNodeBlendTree.
- AnimationNodeStateMachine <- AnimationRootNode — A state machine with multiple AnimationRootNodes, used by AnimationTree.
- AnimationNodeStateMachinePlayback <- Resource — Provides playback control for an AnimationNodeStateMachine.
- AnimationNodeStateMachineTransition <- Resource — A transition within an AnimationNodeStateMachine connecting two AnimationRootNodes.
- AnimationNodeSub2 <- AnimationNodeSync — Blends two animations subtractively inside of an AnimationNodeBlendTree.
- AnimationNodeSync <- AnimationNode — Base class for AnimationNodes with multiple input ports that must be synchronized.
- AnimationNodeTimeScale <- AnimationNode — A time-scaling animation node used in AnimationTree.
- AnimationNodeTimeSeek <- AnimationNode — A time-seeking animation node used in AnimationTree.
- AnimationNodeTransition <- AnimationNodeSync — A transition within an AnimationTree connecting two AnimationNodes.
- AnimationRootNode <- AnimationNode — Base class for AnimationNodes that hold one or multiple composite animations.
- Array — A built-in data structure that holds a sequence of elements.
- ArrayMesh <- Mesh — Mesh type that provides utility for constructing a surface from arrays.
- ArrayOccluder3D <- Occluder3D — 3D polygon shape for use with occlusion culling in OccluderInstance3D.
- AspectRatioContainer <- Container — A container that preserves the proportions of its child controls.
- AtlasTexture <- Texture2D — A texture that crops out part of another Texture2D.
- AudioBusLayout <- Resource — Stores information about the audio buses.
- AudioEffect <- Resource — Base class for audio effect resources.
- AudioEffectAmplify <- AudioEffect — Adds an amplifying audio effect to an audio bus.
- AudioEffectBandLimitFilter <- AudioEffectFilter — Adds a band limit filter to the audio bus.
- AudioEffectBandPassFilter <- AudioEffectFilter — Adds a band pass filter to the audio bus.
- AudioEffectCapture <- AudioEffect — Captures audio from an audio bus in real-time.
- AudioEffectChorus <- AudioEffect — Adds a chorus audio effect.
- AudioEffectCompressor <- AudioEffect — Adds a compressor audio effect to an audio bus.
- AudioEffectDelay <- AudioEffect — Adds a delay audio effect to an audio bus.
- AudioEffectDistortion <- AudioEffect — Adds a distortion audio effect to an Audio bus.
- AudioEffectEQ <- AudioEffect — Base class for audio equalizers.
- AudioEffectEQ10 <- AudioEffectEQ — Adds a 10-band equalizer audio effect to an Audio bus.
- AudioEffectEQ21 <- AudioEffectEQ — Adds a 21-band equalizer audio effect to an Audio bus.
- AudioEffectEQ6 <- AudioEffectEQ — Adds a 6-band equalizer audio effect to an audio bus.
- AudioEffectFilter <- AudioEffect — Adds a filter to the audio bus.
- AudioEffectHardLimiter <- AudioEffect — Adds a hard limiter audio effect to an Audio bus.
- AudioEffectHighPassFilter <- AudioEffectFilter — Adds a high-pass filter to the audio bus.
- AudioEffectHighShelfFilter <- AudioEffectFilter — Adds a high-shelf filter to the audio bus.
- AudioEffectInstance <- RefCounted — Manipulates the audio it receives for a given effect.
- AudioEffectLimiter <- AudioEffect — Adds a soft-clip limiter audio effect to an Audio bus.
- AudioEffectLowPassFilter <- AudioEffectFilter — Adds a low-pass filter to the audio bus.
- AudioEffectLowShelfFilter <- AudioEffectFilter — Adds a low-shelf filter to the audio bus.
- AudioEffectNotchFilter <- AudioEffectFilter — Adds a notch filter to the Audio bus.
- AudioEffectPanner <- AudioEffect — Adds a panner audio effect to an audio bus.
- AudioEffectPhaser <- AudioEffect — Adds a phaser audio effect to an audio bus.
- AudioEffectPitchShift <- AudioEffect — Adds a pitch-shifting audio effect to an audio bus.
- AudioEffectRecord <- AudioEffect — Audio effect used for recording the sound from an audio bus.
- AudioEffectReverb <- AudioEffect — Adds a reverberation audio effect to an Audio bus.
- AudioEffectSpectrumAnalyzer <- AudioEffect — Audio effect that can be used for real-time audio visualizations.
- AudioEffectSpectrumAnalyzerInstance <- AudioEffectInstance — Queryable instance of an AudioEffectSpectrumAnalyzer.
- AudioEffectStereoEnhance <- AudioEffect — An audio effect that can be used to adjust the intensity of stereo panning.
- AudioListener2D <- Node2D — Overrides the location sounds are heard from.
- AudioListener3D <- Node3D — Overrides the location sounds are heard from.
- AudioSample <- RefCounted — Base class for audio samples.
- AudioSamplePlayback <- RefCounted — Meta class for playing back audio samples.
- AudioServer <- Object — Server interface for low-level audio access.
- AudioStreamGenerator <- AudioStream — An audio stream with utilities for procedural sound generation.
- AudioStreamGeneratorPlayback <- AudioStreamPlaybackResampled — Plays back audio generated using AudioStreamGenerator.
- AudioStreamMicrophone <- AudioStream — Plays real-time audio input data.
- AudioStreamPlayback <- RefCounted — Meta class for playing back audio.
- AudioStreamPlaybackPolyphonic <- AudioStreamPlayback — Playback instance for AudioStreamPolyphonic.
- AudioStreamPlaybackResampled <- AudioStreamPlayback
- AudioStreamPolyphonic <- AudioStream — AudioStream that lets the user play custom streams at any time from code, simultaneously using a single player.
- AudioStreamRandomizer <- AudioStream — Wraps a pool of audio streams with pitch and volume shifting.
- AudioStreamWAV <- AudioStream — Stores audio data loaded from WAV files.
- BackBufferCopy <- Node2D — A node that copies a region of the screen to a buffer for access in shader code.
- BaseButton <- Control — Abstract base class for GUI buttons.
- BaseMaterial3D <- Material — Abstract base class for defining the 3D rendering properties of meshes.
- BitMap <- Resource — Boolean matrix.
- BlitMaterial <- Material — A material that processes blit calls to a DrawableTexture.
- Bone2D <- Node2D — A joint used with Skeleton2D to control and animate other nodes.
- BoneAttachment3D <- Node3D — А node that dynamically copies or overrides the 3D transform of a bone in its parent Skeleton3D.
- BoneConstraint3D <- SkeletonModifier3D — A node that may modify Skeleton3D's bone with associating the two bones.
- BoneMap <- Resource — Describes a mapping of bone names for retargeting Skeleton3D into common names defined by a SkeletonProfile.
- BoneTwistDisperser3D <- SkeletonModifier3D — A node that propagates and disperses the child bone's twist to the parent bones.
- BoxContainer <- Container — A container that arranges its child controls horizontally or vertically.
- BoxOccluder3D <- Occluder3D — Cuboid shape for use with occlusion culling in OccluderInstance3D.
- ButtonGroup <- Resource — A group of buttons that doesn't allow more than one button to be pressed at a time.
- CCDIK3D <- IterateIK3D — Rotation based cyclic coordinate descent inverse kinematics solver.
- CallbackTweener <- Tweener — Calls the specified method after optional delay.
- CameraAttributes <- Resource — Parent class for camera settings.
- CameraAttributesPhysical <- CameraAttributes — Physically-based camera settings.
- CameraAttributesPractical <- CameraAttributes — Camera settings in an easy to use format.
- CameraFeed <- RefCounted — A camera feed gives you access to a single physical camera attached to your device.
- CameraTexture <- Texture2D — Texture provided by a CameraFeed.
- CanvasGroup <- Node2D — Merges several 2D nodes into a single draw operation.
- CanvasModulate <- Node2D — A node that applies a color tint to a canvas.
- CanvasTexture <- Texture2D — Texture with optional normal and specular maps for use in 2D rendering.
- ChainIK3D <- IKModifier3D — A SkeletonModifier3D to apply inverse kinematics to bone chains containing an arbitrary number of bones.
- CharFXTransform <- RefCounted — Controls how an individual character will be displayed in a RichTextEffect.
- CheckButton <- Button — A button that represents a binary choice.
- ClassDB <- Object — A class information repository.
- CodeEdit <- TextEdit — A multiline text editor designed for editing code.
- CodeHighlighter <- SyntaxHighlighter — A syntax highlighter intended for code.
- CollisionObject2D <- Node2D — Abstract base class for 2D physics objects.
- CollisionObject3D <- Node3D — Abstract base class for 3D physics objects.
- ColorPalette <- Resource — A resource class for managing a palette of colors, which can be loaded and saved using ColorPicker.
- ColorPicker <- VBoxContainer — A widget that provides an interface for selecting or modifying a color.
- ColorPickerButton <- Button — A button that brings up a ColorPicker when pressed.
- Compositor <- Resource — Stores attributes used to customize how a Viewport is rendered.
- CompositorEffect <- Resource — This resource allows for creating a custom rendering effect.
- CompressedCubemap <- CompressedTextureLayered — An optionally compressed Cubemap.
- CompressedCubemapArray <- CompressedTextureLayered — An optionally compressed CubemapArray.
- CompressedTexture2D <- Texture2D — Texture with 2 dimensions, optionally compressed.
- CompressedTexture2DArray <- CompressedTextureLayered — Array of 2-dimensional textures, optionally compressed.
- CompressedTexture3D <- Texture3D — Texture with 3 dimensions, optionally compressed.
- CompressedTextureLayered <- TextureLayered — Base class for texture arrays that can optionally be compressed.
- ConcavePolygonShape2D <- Shape2D — A 2D polyline shape used for physics collision.
- ConcavePolygonShape3D <- Shape3D — A 3D trimesh shape used for physics collision.
- ConeTwistJoint3D <- Joint3D — A physics joint that connects two 3D physics bodies in a way that simulates a ball-and-socket joint.
- ConfigFile <- RefCounted — Helper class to handle INI-style files.
- ConfirmationDialog <- AcceptDialog — A dialog used for confirmation of actions.
- ConvertTransformModifier3D <- BoneConstraint3D — A SkeletonModifier3D that apply transform to the bone which converted from reference.
- CopyTransformModifier3D <- BoneConstraint3D — A SkeletonModifier3D that apply transform to the bone which copied from reference.
- Crypto <- RefCounted — Provides access to advanced cryptographic functionalities.
- CryptoKey <- Resource — A cryptographic key (RSA or elliptic-curve).
- Cubemap <- ImageTextureLayered — Six square textures representing the faces of a cube.
- CubemapArray <- ImageTextureLayered — An array of Cubemaps, stored together and with a single reference.
- Curve <- Resource — A mathematical curve.
- CurveTexture <- Texture2D — A 1D texture where pixel brightness corresponds to points on a curve.
- CurveXYZTexture <- Texture2D — A 1D texture where the red, green, and blue color channels correspond to points on 3 curves.
- CylinderShape3D <- Shape3D — A 3D cylinder shape used for physics collision.
- DPITexture <- Texture2D — An automatically scalable Texture2D based on an SVG image.
- DampedSpringJoint2D <- Joint2D — A physics joint that connects two 2D physics bodies with a spring-like force.
- Decal <- VisualInstance3D — Node that projects a texture onto a MeshInstance3D.
- Dictionary — A built-in data structure that holds key-value pairs.
- DirAccess <- RefCounted — Provides methods for managing directories and their content.
- DrawableTexture2D <- Texture2D — A 2D texture that supports drawing to itself via Blit calls.
- EncodedObjectAsID <- RefCounted — Holds a reference to an Object's instance ID.
- EngineDebugger <- Object — Exposes the internal debugger.
- EngineProfiler <- RefCounted — Base class for creating custom profilers.
- Environment <- Resource — Resource for environment nodes (like WorldEnvironment) that define multiple rendering options.
- Expression <- RefCounted — A class that stores an expression you can execute.
- ExternalTexture <- Texture2D — Texture which displays the content of an external buffer.
- FABRIK3D <- IterateIK3D — Position based forward and backward reaching inverse kinematics solver.
- FileAccess <- RefCounted — Provides methods for file reading and writing operations.
- FileDialog <- ConfirmationDialog — A dialog for selecting files or directories in the filesystem.
- FileSystemDock <- EditorDock — Godot editor's dock for managing files in the project.
- FlowContainer <- Container — A container that arranges its child controls horizontally or vertically and wraps them around at the borders.
- FogMaterial <- Material — A material that controls how volumetric fog is rendered, to be assigned to a FogVolume.
- FogVolume <- VisualInstance3D — A region that contributes to the default volumetric fog from the world environment.
- FoldableContainer <- Container — A container that can be expanded/collapsed.
- FoldableGroup <- Resource — A group of foldable containers that doesn't allow more than one container to be expanded at a time.
- Font <- Resource — Abstract base class for fonts and font variations.
- FontFile <- Font — Holds font source data and prerendered glyph cache, imported from a dynamic or a bitmap font.
- FontVariation <- Font — A variation of a font with additional settings.
- FramebufferCacheRD <- Object — Framebuffer cache manager for Rendering Device based renderers.
- GDExtension <- Resource — A native library for GDExtension.
- GDExtensionManager <- Object — Provides access to GDExtension functionality.
- GPUParticlesAttractor3D <- VisualInstance3D — Abstract base class for 3D particle attractors.
- GPUParticlesAttractorBox3D <- GPUParticlesAttractor3D — A box-shaped attractor that influences particles from GPUParticles3D nodes.
- GPUParticlesAttractorSphere3D <- GPUParticlesAttractor3D — A spheroid-shaped attractor that influences particles from GPUParticles3D nodes.
- GPUParticlesAttractorVectorField3D <- GPUParticlesAttractor3D — A box-shaped attractor with varying directions and strengths defined in it that influences particles from GPUParticles3D nodes.
- GPUParticlesCollision3D <- VisualInstance3D — Abstract base class for 3D particle collision shapes affecting GPUParticles3D nodes.
- GPUParticlesCollisionBox3D <- GPUParticlesCollision3D — A box-shaped 3D particle collision shape affecting GPUParticles3D nodes.
- GPUParticlesCollisionHeightField3D <- GPUParticlesCollision3D — A real-time heightmap-shaped 3D particle collision shape affecting GPUParticles3D nodes.
- GPUParticlesCollisionSDF3D <- GPUParticlesCollision3D — A baked signed distance field 3D particle collision shape affecting GPUParticles3D nodes.
- GPUParticlesCollisionSphere3D <- GPUParticlesCollision3D — A sphere-shaped 3D particle collision shape affecting GPUParticles3D nodes.
- Generic6DOFJoint3D <- Joint3D — A physics joint that allows for complex movement and rotation between two 3D physics bodies.
- Geometry2D <- Object — Provides methods for some common 2D geometric operations.
- Geometry3D <- Object — Provides methods for some common 3D geometric operations.
- GeometryInstance3D <- VisualInstance3D — Base node for geometry-based visual instances.
- GodotInstance <- Object — Provides access to an embedded Godot instance.
- Gradient <- Resource — A color transition.
- GradientTexture1D <- Texture2D — A 1D texture that uses colors obtained from a Gradient.
- GradientTexture2D <- Texture2D — A 2D texture that creates a pattern with colors obtained from a Gradient.
- GraphEdit <- Control — An editor for graph-like structures, using GraphNodes.
- GraphElement <- Container — A container that represents a basic element that can be placed inside a GraphEdit control.
- GraphFrame <- GraphElement — GraphFrame is a special GraphElement that can be used to organize other GraphElements inside a GraphEdit.
- GraphNode <- GraphElement — A container with connection ports, representing a node in a GraphEdit.
- GrooveJoint2D <- Joint2D — A physics joint that restricts the movement of two 2D physics bodies to a fixed axis.
- HFlowContainer <- FlowContainer — A container that arranges its child controls horizontally and wraps them around at the borders.
- HMACContext <- RefCounted — Used to create an HMAC for a message using a key.
- HScrollBar <- ScrollBar — A horizontal scrollbar that goes from left (min) to right (max).
- HSeparator <- Separator — A horizontal line used for separating other controls.
- HSplitContainer <- SplitContainer — A container that splits two child controls horizontally and provides a grabber for adjusting the split ratio.
- HTTPClient <- RefCounted — Low-level hyper-text transfer protocol client.
- HTTPRequest <- Node — A node with the ability to send HTTP(S) requests.
- HashingContext <- RefCounted — Provides functionality for computing cryptographic hashes chunk by chunk.
- HeightMapShape3D <- Shape3D — A 3D heightmap shape used for physics collision.
- HingeJoint3D <- Joint3D — A physics joint that restricts the rotation of a 3D physics body around an axis relative to another physics body.
- IKModifier3D <- SkeletonModifier3D — A node for inverse kinematics which may modify more than one bone.
- IP <- Object — Internet protocol (IP) support functions such as DNS resolution.
- Image <- Resource — Image datatype.
- ImageFormatLoader <- RefCounted — Base class to add support for specific image formats.
- ImageFormatLoaderExtension <- ImageFormatLoader — Base class for creating ImageFormatLoader extensions (adding support for extra image formats).
- ImageTexture <- Texture2D — A Texture2D based on an Image.
- ImageTexture3D <- Texture3D — Texture with 3 dimensions.
- ImageTextureLayered <- TextureLayered — Base class for texture types which contain the data of multiple ImageTextures.
- ImmediateMesh <- Mesh — Mesh optimized for creating geometry manually.
- ImporterMesh <- Resource — A Resource that contains vertex array-based geometry during the import process.
- ImporterMeshInstance3D <- Node3D
- InputEventAction <- InputEvent — An input event type for actions.
- InputEventFromWindow <- InputEvent — Abstract base class for Viewport-based input events.
- InputEventGesture <- InputEventWithModifiers — Abstract base class for touch gestures.
- InputEventJoypadButton <- InputEvent — Represents a gamepad button being pressed or released.
- InputEventJoypadMotion <- InputEvent — Represents axis motions (such as joystick or analog triggers) from a gamepad.
- InputEventKey <- InputEventWithModifiers — Represents a key on a keyboard being pressed or released.
- InputEventMIDI <- InputEvent — Represents a MIDI message from a MIDI device, such as a musical keyboard.
- InputEventMagnifyGesture <- InputEventGesture — Represents a magnifying touch gesture.
- InputEventMouse <- InputEventWithModifiers — Base input event type for mouse events.
- InputEventMouseButton <- InputEventMouse — Represents a mouse button being pressed or released.
- InputEventMouseMotion <- InputEventMouse — Represents a mouse or a pen movement.
- InputEventPanGesture <- InputEventGesture — Represents a panning touch gesture.
- InputEventScreenDrag <- InputEventFromWindow — Represents a screen drag event.
- InputEventScreenTouch <- InputEventFromWindow — Represents a screen touch event.
- InputEventShortcut <- InputEvent — Represents a triggered keyboard Shortcut.
- InputEventWithModifiers <- InputEventFromWindow — Abstract base class for input events affected by modifier keys like [kbd]Shift[/kbd] and [kbd]Alt[/kbd].
- InputMap <- Object — A singleton that manages all InputEventActions.
- InstancePlaceholder <- Node — Placeholder for the root Node of a PackedScene.
- IntervalTweener <- Tweener — Creates an idle interval in a Tween animation.
- ItemList <- Control — A vertical list of selectable items with one or multiple columns.
- IterateIK3D <- ChainIK3D — A SkeletonModifier3D to approach the goal by repeating small rotations.
- JNISingleton <- Object — Singleton that connects the engine with Android plugins to interface with native Android code.
- JSON <- Resource — Helper class for creating and parsing JSON data.
- JSONRPC <- Object — A helper to handle dictionaries which look like JSONRPC documents.
- JacobianIK3D <- IterateIK3D — Jacobian transpose based inverse kinematics solver.
- JavaClass <- RefCounted — Represents a class from the Java Native Interface.
- JavaClassWrapper <- Object — Provides access to the Java Native Interface.
- JavaObject <- RefCounted — Represents an object from the Java Native Interface.
- JavaScriptBridge <- Object — Singleton that connects the engine with the browser's JavaScript context in Web export.
- JavaScriptObject <- RefCounted — A wrapper class for web native JavaScript objects.
- Joint2D <- Node2D — Abstract base class for all 2D physics joints.
- Joint3D <- Node3D — Abstract base class for all 3D physics joints.
- JointLimitation3D <- Resource — A base class of the limitation that interacts with ChainIK3D.
- JointLimitationCone3D <- JointLimitation3D — A cone shape limitation that interacts with ChainIK3D.
- KinematicCollision2D <- RefCounted — Holds collision data from the movement of a PhysicsBody2D.
- KinematicCollision3D <- RefCounted — Holds collision data from the movement of a PhysicsBody3D.
- Label3D <- GeometryInstance3D — A node for displaying plain text in 3D space.
- LabelSettings <- Resource — Provides common settings to customize the text in a Label.
- LightOccluder2D <- Node2D — Occludes light cast by a Light2D, casting shadows.
- LightmapGI <- VisualInstance3D — Computes and stores baked lightmaps for fast global illumination.
- LightmapGIData <- Resource — Contains baked lightmap and dynamic object probe data for LightmapGI.
- LightmapProbe <- Node3D — Represents a single manually placed probe for dynamic object lighting with LightmapGI.
- Lightmapper <- RefCounted — Abstract class extended by lightmappers, for use in LightmapGI.
- LightmapperRD <- Lightmapper — The built-in GPU-based lightmapper for use with LightmapGI.
- LimitAngularVelocityModifier3D <- SkeletonModifier3D — Limit bone rotation angular velocity.
- Line2D <- Node2D — A 2D polyline that can optionally be textured.
- LinkButton <- BaseButton — A button that represents a link.
- Logger <- RefCounted — Custom logger to receive messages from the internal error/warning stream.
- LookAtModifier3D <- SkeletonModifier3D — The LookAtModifier3D rotates a bone to look at a target.
- MainLoop <- Object — Abstract base class for the game's main loop.
- Marker2D <- Node2D — Generic 2D position hint for editing.
- Marker3D <- Node3D — Generic 3D position hint for editing.
- Marshalls <- Object — Data transformation (marshaling) and encoding helpers.
- MenuBar <- Control — A horizontal menu bar that creates a menu for each PopupMenu child.
- MenuButton <- Button — A button that brings up a PopupMenu when clicked.
- MeshConvexDecompositionSettings <- RefCounted — Parameters to be used with a Mesh convex decomposition operation.
- MeshDataTool <- RefCounted — Helper tool to access and edit Mesh data.
- MeshLibrary <- Resource — Library of meshes.
- MeshTexture <- Texture2D — Simple texture that uses a mesh to draw itself.
- MethodTweener <- Tweener — Interpolates an abstract value and supplies it to a method called over time.
- MissingNode <- Node — An internal editor class intended for keeping the data of unrecognized nodes.
- MissingResource <- Resource — An internal editor class intended for keeping the data of unrecognized resources.
- ModifierBoneTarget3D <- SkeletonModifier3D — А node that dynamically copies the 3D transform of a bone in its parent Skeleton3D.
- MovieWriter <- Object — Abstract class for non-real-time video recording encoders.
- MultiMesh <- Resource — Provides high-performance drawing of a mesh multiple times using GPU instancing.
- MultiplayerAPI <- RefCounted — High-level multiplayer API interface.
- MultiplayerAPIExtension <- MultiplayerAPI — Base class used for extending the MultiplayerAPI.
- MultiplayerPeer <- PacketPeer — Abstract class for specialized PacketPeers used by the MultiplayerAPI.
- MultiplayerPeerExtension <- MultiplayerPeer — Class that can be inherited to implement custom multiplayer API networking layers via GDExtension.
- Mutex <- RefCounted — A binary Semaphore for synchronization of multiple Threads.
- NativeMenu <- Object — A server interface for OS native menus.
- NavigationLink2D <- Node2D — A link between two positions on NavigationRegion2Ds that agents can be routed through.
- NavigationLink3D <- Node3D — A link between two positions on NavigationRegion3Ds that agents can be routed through.
- NavigationMesh <- Resource — A navigation mesh that defines traversable areas and obstacles.
- NavigationMeshGenerator <- Object — Helper class for creating and clearing navigation meshes.
- NavigationMeshSourceGeometryData2D <- Resource — Container for parsed source geometry data used in navigation mesh baking.
- NavigationMeshSourceGeometryData3D <- Resource — Container for parsed source geometry data used in navigation mesh baking.
- NavigationObstacle2D <- Node2D — 2D obstacle used to affect navigation mesh baking or constrain velocities of avoidance controlled agents.
- NavigationObstacle3D <- Node3D — 3D obstacle used to affect navigation mesh baking or constrain velocities of avoidance controlled agents.
- NavigationPathQueryParameters2D <- RefCounted — Provides parameters for 2D navigation path queries.
- NavigationPathQueryParameters3D <- RefCounted — Provides parameters for 3D navigation path queries.
- NavigationPathQueryResult2D <- RefCounted — Represents the result of a 2D pathfinding query.
- NavigationPathQueryResult3D <- RefCounted — Represents the result of a 3D pathfinding query.
- NavigationPolygon <- Resource — A 2D navigation mesh that describes a traversable surface for pathfinding.
- NavigationServer2D <- Object — A server interface for low-level 2D navigation access.
- NavigationServer2DManager <- Object — A singleton for managing NavigationServer2D implementations.
- NavigationServer3D <- Object — A server interface for low-level 3D navigation access.
- NavigationServer3DManager <- Object — A singleton for managing NavigationServer3D implementations.
- NinePatchRect <- Control — A control that displays a texture by keeping its corners intact, but tiling its edges and center.
- Node3DGizmo <- RefCounted — Abstract class to expose editor gizmos for Node3D.
- NodePath — A pre-parsed scene tree path.
- ORMMaterial3D <- BaseMaterial3D — A PBR (Physically Based Rendering) material to be used on 3D objects.
- OS <- Object — Provides access to common operating system functionalities.
- Occluder3D <- Resource — Occluder shape resource for use with occlusion culling in OccluderInstance3D.
- OccluderInstance3D <- VisualInstance3D — Provides occlusion culling for 3D nodes, which improves performance in closed areas.
- OccluderPolygon2D <- Resource — Defines a 2D polygon for LightOccluder2D.
- OptimizedTranslation <- Translation — An optimized translation.
- OptionButton <- Button — A button that brings up a dropdown with selectable options when pressed.
- PCKPacker <- RefCounted — Creates packages that can be loaded into a running project.
- PackedByteArray — A packed array of bytes.
- PackedColorArray — A packed array of Colors.
- PackedDataContainer <- Resource — Efficiently packs and serializes Array or Dictionary.
- PackedDataContainerRef <- RefCounted — An internal class used by PackedDataContainer to pack nested arrays and dictionaries.
- PackedFloat32Array — A packed array of 32-bit floating-point values.
- PackedFloat64Array — A packed array of 64-bit floating-point values.
- PackedInt32Array — A packed array of 32-bit integers.
- PackedInt64Array — A packed array of 64-bit integers.
- PackedStringArray — A packed array of Strings.
- PackedVector2Array — A packed array of Vector2s.
- PackedVector3Array — A packed array of Vector3s.
- PackedVector4Array — A packed array of Vector4s.
- PacketPeer <- RefCounted — Abstraction and base class for packet-based protocols.
- PacketPeerDTLS <- PacketPeer — DTLS packet peer.
- PacketPeerExtension <- PacketPeer
- PacketPeerStream <- PacketPeer — Wrapper to use a PacketPeer over a StreamPeer.
- PacketPeerUDP <- PacketPeer — UDP packet peer.
- PanoramaSkyMaterial <- Material — A material that provides a special texture to a Sky, usually an HDR panorama.
- Parallax2D <- Node2D — A node used to create a parallax scrolling background.
- ParallaxBackground <- CanvasLayer — A node used to create a parallax scrolling background.
- ParallaxLayer <- Node2D — A parallax scrolling layer to be used with ParallaxBackground.
- ParticleProcessMaterial <- Material — Holds a particle configuration for GPUParticles2D or GPUParticles3D nodes.
- Performance <- Object — Exposes performance-related data.
- PhysicalBone2D <- RigidBody2D — A RigidBody2D-derived node used to make Bone2Ds in a Skeleton2D react to physics.
- PhysicalBone3D <- PhysicsBody3D — A physics body used to make bones in a Skeleton3D react to physics.
- PhysicalBoneSimulator3D <- SkeletonModifier3D — Node that can be the parent of PhysicalBone3D and can apply the simulation results to Skeleton3D.
- PhysicalSkyMaterial <- Material — A material that defines a sky for a Sky resource by a set of physical properties.
- PhysicsBody2D <- CollisionObject2D — Abstract base class for 2D game objects affected by physics.
- PhysicsBody3D <- CollisionObject3D — Abstract base class for 3D game objects affected by physics.
- PhysicsDirectBodyState2D <- Object — Provides direct access to a physics body in the PhysicsServer2D.
- PhysicsDirectBodyState2DExtension <- PhysicsDirectBodyState2D — Provides virtual methods that can be overridden to create custom PhysicsDirectBodyState2D implementations.
- PhysicsDirectBodyState3D <- Object — Provides direct access to a physics body in the PhysicsServer3D.
- PhysicsDirectBodyState3DExtension <- PhysicsDirectBodyState3D — Provides virtual methods that can be overridden to create custom PhysicsDirectBodyState3D implementations.
- PhysicsDirectSpaceState2D <- Object — Provides direct access to a physics space in the PhysicsServer2D.
- PhysicsDirectSpaceState2DExtension <- PhysicsDirectSpaceState2D — Provides virtual methods that can be overridden to create custom PhysicsDirectSpaceState2D implementations.
- PhysicsDirectSpaceState3D <- Object — Provides direct access to a physics space in the PhysicsServer3D.
- PhysicsDirectSpaceState3DExtension <- PhysicsDirectSpaceState3D — Provides virtual methods that can be overridden to create custom PhysicsDirectSpaceState3D implementations.
- PhysicsMaterial <- Resource — Holds physics-related properties of a surface, namely its roughness and bounciness.
- PhysicsPointQueryParameters2D <- RefCounted — Provides parameters for `PhysicsDirectSpaceState2D.
- PhysicsPointQueryParameters3D <- RefCounted — Provides parameters for `PhysicsDirectSpaceState3D.
- PhysicsRayQueryParameters2D <- RefCounted — Provides parameters for `PhysicsDirectSpaceState2D.
- PhysicsRayQueryParameters3D <- RefCounted — Provides parameters for `PhysicsDirectSpaceState3D.
- PhysicsServer2D <- Object — A server interface for low-level 2D physics access.
- PhysicsServer2DExtension <- PhysicsServer2D — Provides virtual methods that can be overridden to create custom PhysicsServer2D implementations.
- PhysicsServer2DManager <- Object — A singleton for managing PhysicsServer2D implementations.
- PhysicsServer3D <- Object — A server interface for low-level 3D physics access.
- PhysicsServer3DExtension <- PhysicsServer3D — Provides virtual methods that can be overridden to create custom PhysicsServer3D implementations.
- PhysicsServer3DManager <- Object — A singleton for managing PhysicsServer3D implementations.
- PhysicsServer3DRenderingServerHandler <- Object — A class used to provide `PhysicsServer3DExtension.
- PhysicsShapeQueryParameters2D <- RefCounted — Provides parameters for PhysicsDirectSpaceState2D's methods.
- PhysicsShapeQueryParameters3D <- RefCounted — Provides parameters for PhysicsDirectSpaceState3D's methods.
- PhysicsTestMotionParameters2D <- RefCounted — Provides parameters for `PhysicsServer2D.
- PhysicsTestMotionParameters3D <- RefCounted — Provides parameters for `PhysicsServer3D.
- PhysicsTestMotionResult2D <- RefCounted — Describes the motion and collision result from `PhysicsServer2D.
- PhysicsTestMotionResult3D <- RefCounted — Describes the motion and collision result from `PhysicsServer3D.
- PinJoint2D <- Joint2D — A physics joint that attaches two 2D physics bodies at a single point, allowing them to freely rotate.
- PinJoint3D <- Joint3D — A physics joint that attaches two 3D physics bodies at a single point, allowing them to freely rotate.
- PlaceholderCubemap <- PlaceholderTextureLayered — A Cubemap without image data.
- PlaceholderCubemapArray <- PlaceholderTextureLayered — A CubemapArray without image data.
- PlaceholderMaterial <- Material — Placeholder class for a material.
- PlaceholderMesh <- Mesh — Placeholder class for a mesh.
- PlaceholderTexture2D <- Texture2D — Placeholder class for a 2-dimensional texture.
- PlaceholderTexture2DArray <- PlaceholderTextureLayered — Placeholder class for a 2-dimensional texture array.
- PlaceholderTexture3D <- Texture3D — Placeholder class for a 3-dimensional texture.
- PlaceholderTextureLayered <- TextureLayered — Placeholder class for a 2-dimensional texture array.
- PointMesh <- PrimitiveMesh — Mesh with a single point primitive.
- Polygon2D <- Node2D — A 2D polygon.
- PolygonOccluder3D <- Occluder3D — Flat 2D polygon shape for use with occlusion culling in OccluderInstance3D.
- PolygonPathFinder <- Resource
- Popup <- Window — Base class for contextual windows and panels with fixed position.
- PopupMenu <- Popup — A modal window used to display a list of options.
- PopupPanel <- Popup — A popup with a panel background.
- PortableCompressedTexture2D <- Texture2D — Provides a compressed texture for disk and/or VRAM in a way that is portable.
- PrismMesh <- PrimitiveMesh — Class representing a prism-shaped PrimitiveMesh.
- ProceduralSkyMaterial <- Material — A material that defines a simple sky for a Sky resource.
- ProjectSettings <- Object — Stores globally-accessible variables.
- Projection — A 4×4 matrix for 3D projective transformations.
- PropertyTweener <- Tweener — Interpolates an Object's property over time.
- QuadMesh <- PlaneMesh — Class representing a square mesh facing the camera.
- QuadOccluder3D <- Occluder3D — Flat plane shape for use with occlusion culling in OccluderInstance3D.
- RDAttachmentFormat <- RefCounted — Attachment format (used by RenderingDevice).
- RDFramebufferPass <- RefCounted — Framebuffer pass attachment description (used by RenderingDevice).
- RDPipelineColorBlendState <- RefCounted — Pipeline color blend state (used by RenderingDevice).
- RDPipelineColorBlendStateAttachment <- RefCounted — Pipeline color blend state attachment (used by RenderingDevice).
- RDPipelineDepthStencilState <- RefCounted — Pipeline depth/stencil state (used by RenderingDevice).
- RDPipelineMultisampleState <- RefCounted — Pipeline multisample state (used by RenderingDevice).
- RDPipelineRasterizationState <- RefCounted — Pipeline rasterization state (used by RenderingDevice).
- RDPipelineSpecializationConstant <- RefCounted — Pipeline specialization constant (used by RenderingDevice).
- RDSamplerState <- RefCounted — Sampler state (used by RenderingDevice).
- RDShaderFile <- Resource — Compiled shader file in SPIR-V form (used by RenderingDevice).
- RDShaderSPIRV <- Resource — SPIR-V intermediate representation as part of an RDShaderFile (used by RenderingDevice).
- RDShaderSource <- RefCounted — Shader source code (used by RenderingDevice).
- RDTextureFormat <- RefCounted — Texture format (used by RenderingDevice).
- RDTextureView <- RefCounted — Texture view (used by RenderingDevice).
- RDUniform <- RefCounted — Shader uniform (used by RenderingDevice).
- RDVertexAttribute <- RefCounted — Vertex attribute (used by RenderingDevice).
- RID — A handle for a Resource's unique identifier.
- RandomNumberGenerator <- RefCounted — Provides methods for generating pseudo-random numbers.
- Range <- Control — Abstract base class for controls that represent a number within a range.
- Rect2i — A 2D axis-aligned bounding box using integer coordinates.
- ReferenceRect <- Control — A rectangular box for designing UIs.
- ReflectionProbe <- VisualInstance3D — Captures its surroundings to create fast, accurate reflections from a given point.
- RemoteTransform2D <- Node2D — RemoteTransform2D pushes its own Transform2D to another Node2D derived node in the scene.
- RemoteTransform3D <- Node3D — RemoteTransform3D pushes its own Transform3D to another Node3D derived Node in the scene.
- RenderData <- Object — Abstract render data object, holds frame data related to rendering a single frame of a viewport.
- RenderDataExtension <- RenderData — This class allows for a RenderData implementation to be made in GDExtension.
- RenderDataRD <- RenderData — Render data implementation for the RenderingDevice based renderers.
- RenderSceneBuffers <- RefCounted — Abstract scene buffers object, created for each viewport for which 3D rendering is done.
- RenderSceneBuffersConfiguration <- RefCounted — Configuration object used to setup a RenderSceneBuffers object.
- RenderSceneBuffersExtension <- RenderSceneBuffers — This class allows for a RenderSceneBuffer implementation to be made in GDExtension.
- RenderSceneBuffersRD <- RenderSceneBuffers — Render scene buffer implementation for the RenderingDevice based renderers.
- RenderSceneData <- Object — Abstract render data object, holds scene data related to rendering a single frame of a viewport.
- RenderSceneDataExtension <- RenderSceneData — This class allows for a RenderSceneData implementation to be made in GDExtension.
- RenderSceneDataRD <- RenderSceneData — Render scene data implementation for the RenderingDevice based renderers.
- RenderingDevice <- Object — Abstraction for working with modern low-level graphics APIs.
- ResourceFormatLoader <- RefCounted — Loads a specific resource type from a file.
- ResourceFormatSaver <- RefCounted — Saves a specific resource type to a file.
- ResourceImporter <- RefCounted — Base class for resource importers.
- ResourceImporterBMFont <- ResourceImporter — Imports a bitmap font in the BMFont (`.
- ResourceImporterBitMap <- ResourceImporter — Imports a BitMap resource (2D array of boolean values).
- ResourceImporterCSVTranslation <- ResourceImporter — Imports comma-separated values as Translations.
- ResourceImporterDynamicFont <- ResourceImporter — Imports a TTF, TTC, OTF, OTC, WOFF or WOFF2 font file for font rendering that adapts to any size.
- ResourceImporterImage <- ResourceImporter — Imports an image for use in scripting, with no rendering capabilities.
- ResourceImporterImageFont <- ResourceImporter — Imports a bitmap font where all glyphs have the same width and height.
- ResourceImporterLayeredTexture <- ResourceImporter — Imports a 3-dimensional texture (Texture3D), a Texture2DArray, a Cubemap or a CubemapArray.
- ResourceImporterOBJ <- ResourceImporter — Imports an OBJ 3D model as an independent Mesh or scene.
- ResourceImporterSVG <- ResourceImporter — Imports an SVG file as an automatically scalable texture for use in UI elements and 2D rendering.
- ResourceImporterScene <- ResourceImporter — Imports a glTF, FBX, COLLADA, or Blender 3D scene.
- ResourceImporterShaderFile <- ResourceImporter — Imports native GLSL shaders (not Godot shaders) as an RDShaderFile.
- ResourceImporterTexture <- ResourceImporter — Imports an image for use in 2D or 3D rendering.
- ResourceImporterTextureAtlas <- ResourceImporter — Imports a collection of textures from a PNG image into an optimized AtlasTexture for 2D rendering.
- ResourceImporterWAV <- ResourceImporter — Imports a WAV audio file for playback.
- ResourceLoader <- Object — A singleton for loading resource files.
- ResourcePreloader <- Node — A node used to preload sub-resources inside a scene.
- ResourceSaver <- Object — A singleton for saving Resources to the filesystem.
- ResourceUID <- Object — A singleton that manages the unique identifiers of all resources within a project.
- RetargetModifier3D <- SkeletonModifier3D — A modifier to transfer parent skeleton poses (or global poses) to child skeletons in model space with different rests.
- RibbonTrailMesh <- PrimitiveMesh — Represents a straight ribbon-shaped PrimitiveMesh with variable width.
- RichTextEffect <- Resource — A custom effect for a RichTextLabel.
- RichTextLabel <- Control — A control for displaying text that can contain different font styles, images, and basic formatting.
- RootMotionView <- VisualInstance3D — Editor-only helper for setting up root motion in AnimationMixer.
- SceneState <- RefCounted — Provides access to a scene file's information.
- SceneTreeTimer <- RefCounted — One-shot timer.
- Script <- Resource — A class stored as a resource.
- ScriptBacktrace <- RefCounted — A captured backtrace of a specific script language.
- ScriptCreateDialog <- ConfirmationDialog — Godot editor's popup dialog for creating new Script files.
- ScriptEditor <- PanelContainer — Godot editor's script editor.
- ScriptEditorBase <- Control — Base editor for editing scripts in the ScriptEditor.
- ScriptExtension <- Script
- ScriptLanguage <- Object
- ScriptLanguageExtension <- ScriptLanguage
- ScrollBar <- Range — Abstract base class for scrollbars.
- SegmentShape2D <- Shape2D — A 2D line segment shape used for physics collision.
- Semaphore <- RefCounted — A synchronization mechanism used to control access to a shared resource by Threads.
- SeparationRayShape2D <- Shape2D — A 2D ray shape used for physics collision that tries to separate itself from any collider.
- SeparationRayShape3D <- Shape3D — A 3D ray shape used for physics collision that tries to separate itself from any collider.
- Separator <- Control — Abstract base class for separators.
- Shader <- Resource — A shader implemented in the Godot shading language.
- ShaderGlobalsOverride <- Node — A node used to override global shader parameters' values in a scene.
- ShaderInclude <- Resource — A snippet of shader code to be included in a Shader with `#include`.
- ShaderIncludeDB <- Object — Internal database of built in shader include files.
- Shortcut <- Resource — A shortcut for binding input.
- Skeleton2D <- Node2D — The parent of a hierarchy of Bone2Ds, used to create a 2D skeletal animation.
- Skeleton3D <- Node3D — A node containing a bone hierarchy, used to create a 3D skeletal animation.
- SkeletonIK3D <- SkeletonModifier3D — A node used to rotate all bones of a Skeleton3D bone chain a way that places the end bone at a desired 3D position.
- SkeletonModification2D <- Resource — Base class for resources that operate on Bone2Ds in a Skeleton2D.
- SkeletonModification2DCCDIK <- SkeletonModification2D — A modification that uses CCDIK to manipulate a series of bones to reach a target in 2D.
- SkeletonModification2DFABRIK <- SkeletonModification2D — A modification that uses FABRIK to manipulate a series of Bone2D nodes to reach a target.
- SkeletonModification2DJiggle <- SkeletonModification2D — A modification that jiggles Bone2D nodes as they move towards a target.
- SkeletonModification2DLookAt <- SkeletonModification2D — A modification that rotates a Bone2D node to look at a target.
- SkeletonModification2DPhysicalBones <- SkeletonModification2D — A modification that applies the transforms of PhysicalBone2D nodes to Bone2D nodes.
- SkeletonModification2DStackHolder <- SkeletonModification2D — A modification that holds and executes a SkeletonModificationStack2D.
- SkeletonModification2DTwoBoneIK <- SkeletonModification2D — A modification that rotates two bones using the law of cosines to reach the target.
- SkeletonModificationStack2D <- Resource — A resource that holds a stack of SkeletonModification2Ds.
- SkeletonModifier3D <- Node3D — A node that may modify a Skeleton3D's bones.
- SkeletonProfile <- Resource — Base class for a profile of a virtual skeleton used as a target for retargeting.
- SkeletonProfileHumanoid <- SkeletonProfile — A humanoid SkeletonProfile preset.
- Skin <- Resource
- SkinReference <- RefCounted — A reference-counted holder object for a skeleton RID used in the RenderingServer.
- Sky <- Resource — Defines a 3D environment's background by using a Material.
- SliderJoint3D <- Joint3D — A physics joint that restricts the movement of a 3D physics body along an axis relative to another physics body.
- SoftBody3D <- MeshInstance3D — A deformable 3D physics mesh.
- SphereOccluder3D <- Occluder3D — Spherical shape for use with occlusion culling in OccluderInstance3D.
- SpinBox <- Range — An input field for numbers.
- SplineIK3D <- ChainIK3D — A SkeletonModifier3D for aligning bones along a Path3D.
- SplitContainer <- Container — A container that arranges child controls horizontally or vertically and provides grabbers for adjusting the split ratios between them.
- SpringArm3D <- Node3D — A 3D raycast that dynamically moves its children near the collision point.
- SpringBoneCollision3D <- Node3D — A base class of the collision that interacts with SpringBoneSimulator3D.
- SpringBoneCollisionCapsule3D <- SpringBoneCollision3D — A capsule shape collision that interacts with SpringBoneSimulator3D.
- SpringBoneCollisionPlane3D <- SpringBoneCollision3D — An infinite plane collision that interacts with SpringBoneSimulator3D.
- SpringBoneCollisionSphere3D <- SpringBoneCollision3D — A sphere shape collision that interacts with SpringBoneSimulator3D.
- SpringBoneSimulator3D <- SkeletonModifier3D — A SkeletonModifier3D to apply inertial wavering to bone chains.
- SpriteBase3D <- GeometryInstance3D — 2D sprite node in 3D environment.
- SpriteFrames <- Resource — Sprite frame library for AnimatedSprite2D and AnimatedSprite3D.
- StatusIndicator <- Node — Application status indicator (aka notification area icon).
- StreamPeer <- RefCounted — Abstract base class for interacting with streams.
- StreamPeerBuffer <- StreamPeer — A stream peer used to handle binary data streams.
- StreamPeerExtension <- StreamPeer
- StreamPeerGZIP <- StreamPeer — A stream peer that handles GZIP and deflate compression/decompression.
- StreamPeerSocket <- StreamPeer — Abstract base class for interacting with socket streams.
- StreamPeerTCP <- StreamPeerSocket — A stream peer that handles TCP connections.
- StreamPeerTLS <- StreamPeer — A stream peer that handles TLS connections.
- StreamPeerUDS <- StreamPeerSocket — A stream peer that handles UNIX Domain Socket (UDS) connections.
- String — A built-in type for strings.
- StringName — A built-in type for unique strings.
- StyleBox <- Resource — Abstract base class for defining stylized boxes for UI elements.
- StyleBoxEmpty <- StyleBox — An empty StyleBox (does not display anything).
- StyleBoxFlat <- StyleBox — A customizable StyleBox that doesn't use a texture.
- StyleBoxLine <- StyleBox — A StyleBox that displays a single line of a given color and thickness.
- StyleBoxTexture <- StyleBox — A texture-based nine-patch StyleBox.
- SubViewport <- Viewport — An interface to a game world that doesn't create a window or draw to the screen directly.
- SubViewportContainer <- Container — A container used for displaying the contents of a SubViewport.
- SubtweenTweener <- Tweener — Runs a Tween nested within another Tween.
- SurfaceTool <- RefCounted — Helper tool to create geometry.
- SyntaxHighlighter <- Resource — Base class for syntax highlighters.
- SystemFont <- Font — A font loaded from a system font.
- TLSOptions <- RefCounted — TLS configuration for clients and servers.
- TabBar <- Control — A control that provides a horizontal bar with tabs.
- TabContainer <- Container — A container that creates a tab for each child control, displaying only the active tab's control.
- TextEdit <- Control — A multiline text editor.
- TextLine <- RefCounted — Holds a line of text.
- TextMesh <- PrimitiveMesh — Generate a PrimitiveMesh from the text.
- TextParagraph <- RefCounted — Holds a paragraph of text.
- TextServerDummy <- TextServerExtension — A dummy text server that can't render text or manage fonts.
- TextServerExtension <- TextServer — Base class for custom TextServer implementations (plugins).
- TextServerManager <- Object — A singleton for managing TextServer implementations.
- Texture <- Resource — Base class for all texture types.
- Texture2DArray <- ImageTextureLayered — A single texture resource which consists of multiple, separate images.
- Texture2DArrayRD <- TextureLayeredRD — Texture Array for 2D that is bound to a texture created on the RenderingDevice.
- Texture2DRD <- Texture2D — Texture for 2D that is bound to a texture created on the RenderingDevice.
- Texture3D <- Texture — Base class for 3-dimensional textures.
- Texture3DRD <- Texture3D — Texture for 3D that is bound to a texture created on the RenderingDevice.
- TextureCubemapArrayRD <- TextureLayeredRD — Texture Array for Cubemaps that is bound to a texture created on the RenderingDevice.
- TextureCubemapRD <- TextureLayeredRD — Texture for Cubemap that is bound to a texture created on the RenderingDevice.
- TextureLayered <- Texture — Base class for texture types which contain the data of multiple Images.
- TextureLayeredRD <- TextureLayered — Abstract base class for layered texture RD types.
- TextureProgressBar <- Range — Texture-based progress bar.
- Theme <- Resource — A resource used for styling/skinning Controls and Windows.
- ThemeDB <- Object — A singleton that provides access to static information about Theme resources used by the engine and by your project.
- Thread <- RefCounted — A unit of execution in a process.
- TileData <- Object — Settings for a single tile in a TileSet.
- TileMap <- Node2D — Node for 2D tile-based maps.
- TileMapPattern <- Resource — Holds a pattern to be copied from or pasted into TileMaps.
- TileSetAtlasSource <- TileSetSource — Exposes a 2D atlas texture as a set of tiles for a TileSet resource.
- TileSetScenesCollectionSource <- TileSetSource — Exposes a set of scenes as tiles for a TileSet resource.
- TileSetSource <- Resource — Exposes a set of tiles for a TileSet resource.
- Time <- Object — A singleton for working with time data.
- TorusMesh <- PrimitiveMesh — Class representing a torus PrimitiveMesh.
- TouchScreenButton <- Node2D — Button for touch screen devices for gameplay use.
- Translation <- Resource — A language translation that maps a collection of strings to their individual translations.
- TranslationDomain <- RefCounted — A self-contained collection of Translation resources.
- Tree <- Control — A control used to show a set of internal TreeItems in a hierarchical structure.
- TreeItem <- Object — An internal control for a single item inside Tree.
- TriangleMesh <- RefCounted — Triangle geometry for efficient, physicsless intersection queries.
- TubeTrailMesh <- PrimitiveMesh — Represents a straight tube-shaped PrimitiveMesh with variable width.
- Tweener <- RefCounted — Abstract class for all Tweeners used by Tween.
- TwoBoneIK3D <- IKModifier3D — Rotation based intersection of two circles inverse kinematics solver.
- UndoRedo <- Object — Provides a high-level interface for implementing undo and redo operations.
- UniformSetCacheRD <- Object — Uniform set cache manager for Rendering Device based renderers.
- VFlowContainer <- FlowContainer — A container that arranges its child controls vertically and wraps them around at the borders.
- VScrollBar <- ScrollBar — A vertical scrollbar that goes from top (min) to bottom (max).
- VSeparator <- Separator — A vertical line used for separating other controls.
- VSplitContainer <- SplitContainer — A container that splits two child controls vertically and provides a grabber for adjusting the split ratio.
- Variant — The most important data type in Godot.
- Vector2i — A 2D vector using integer coordinates.
- Vector3i — A 3D vector using integer coordinates.
- Vector4i — A 4D vector using integer coordinates.
- VehicleBody3D <- RigidBody3D — A 3D physics body that simulates the behavior of a car.
- VehicleWheel3D <- Node3D — A 3D physics body for a VehicleBody3D that simulates the behavior of a wheel.
- VideoStream <- Resource — Base resource for video streams.
- VideoStreamPlayback <- Resource — Internal class used by VideoStream to manage playback state when played from a VideoStreamPlayer.
- VideoStreamPlayer <- Control — A control used for video playback.
- Viewport <- Node — Abstract base class for viewports.
- ViewportTexture <- Texture2D — Provides the content of a Viewport as a dynamic texture.
- VirtualJoystick <- Control — A virtual joystick control for touchscreen devices.
- VisibleOnScreenEnabler2D <- VisibleOnScreenNotifier2D — A rectangular region of 2D space that, when visible on screen, enables a target node.
- VisibleOnScreenEnabler3D <- VisibleOnScreenNotifier3D — A box-shaped region of 3D space that, when visible on screen, enables a target node.
- VisibleOnScreenNotifier2D <- Node2D — A rectangular region of 2D space that detects whether it is visible on screen.
- VisibleOnScreenNotifier3D <- VisualInstance3D — A box-shaped region of 3D space that detects whether it is visible on screen.
- VisualInstance3D <- Node3D — Parent of all visual 3D nodes.
- VisualShader <- Shader — A custom shader program with a visual editor.
- VisualShaderNode <- Resource — Base class for VisualShader nodes.
- VisualShaderNodeBillboard <- VisualShaderNode — A node that controls how the object faces the camera to be used within the visual shader graph.
- VisualShaderNodeBooleanConstant <- VisualShaderNodeConstant — A boolean constant to be used within the visual shader graph.
- VisualShaderNodeBooleanParameter <- VisualShaderNodeParameter — A boolean parameter to be used within the visual shader graph.
- VisualShaderNodeClamp <- VisualShaderNode — Clamps a value within the visual shader graph.
- VisualShaderNodeColorConstant <- VisualShaderNodeConstant — A Color constant to be used within the visual shader graph.
- VisualShaderNodeColorFunc <- VisualShaderNode — A Color function to be used within the visual shader graph.
- VisualShaderNodeColorOp <- VisualShaderNode — A Color operator to be used within the visual shader graph.
- VisualShaderNodeColorParameter <- VisualShaderNodeParameter — A Color parameter to be used within the visual shader graph.
- VisualShaderNodeComment <- VisualShaderNodeFrame — Only exists for compatibility.
- VisualShaderNodeCompare <- VisualShaderNode — A comparison function for common types within the visual shader graph.
- VisualShaderNodeConstant <- VisualShaderNode — A base type for the constants within the visual shader graph.
- VisualShaderNodeCubemap <- VisualShaderNode — A Cubemap sampling node to be used within the visual shader graph.
- VisualShaderNodeCubemapParameter <- VisualShaderNodeTextureParameter — A Cubemap parameter node to be used within the visual shader graph.
- VisualShaderNodeCurveTexture <- VisualShaderNodeResizableBase — Performs a CurveTexture lookup within the visual shader graph.
- VisualShaderNodeCurveXYZTexture <- VisualShaderNodeResizableBase — Performs a CurveXYZTexture lookup within the visual shader graph.
- VisualShaderNodeCustom <- VisualShaderNode — Virtual class to define custom VisualShaderNodes for use in the Visual Shader Editor.
- VisualShaderNodeDerivativeFunc <- VisualShaderNode — Calculates a derivative within the visual shader graph.
- VisualShaderNodeDeterminant <- VisualShaderNode — Calculates the determinant of a Transform3D within the visual shader graph.
- VisualShaderNodeDistanceFade <- VisualShaderNode — A visual shader node representing distance fade effect.
- VisualShaderNodeDotProduct <- VisualShaderNode — Calculates a dot product of two vectors within the visual shader graph.
- VisualShaderNodeExpression <- VisualShaderNodeGroupBase — A custom visual shader graph expression written in Godot Shading Language.
- VisualShaderNodeFaceForward <- VisualShaderNodeVectorBase — Returns the vector that points in the same direction as a reference vector within the visual shader graph.
- VisualShaderNodeFloatConstant <- VisualShaderNodeConstant — A scalar floating-point constant to be used within the visual shader graph.
- VisualShaderNodeFloatFunc <- VisualShaderNode — A scalar floating-point function to be used within the visual shader graph.
- VisualShaderNodeFloatOp <- VisualShaderNode — A floating-point scalar operator to be used within the visual shader graph.
- VisualShaderNodeFloatParameter <- VisualShaderNodeParameter — A scalar float parameter to be used within the visual shader graph.
- VisualShaderNodeFrame <- VisualShaderNodeResizableBase — A frame other visual shader nodes can be attached to for better organization.
- VisualShaderNodeFresnel <- VisualShaderNode — A Fresnel effect to be used within the visual shader graph.
- VisualShaderNodeGlobalExpression <- VisualShaderNodeExpression — A custom global visual shader graph expression written in Godot Shading Language.
- VisualShaderNodeGroupBase <- VisualShaderNodeResizableBase — Base class for a family of nodes with variable number of input and output ports within the visual shader graph.
- VisualShaderNodeIf <- VisualShaderNode — Outputs a 3D vector based on the result of a floating-point comparison within the visual shader graph.
- VisualShaderNodeInput <- VisualShaderNode — Represents the input shader parameter within the visual shader graph.
- VisualShaderNodeIntConstant <- VisualShaderNodeConstant — A scalar integer constant to be used within the visual shader graph.
- VisualShaderNodeIntFunc <- VisualShaderNode — A scalar integer function to be used within the visual shader graph.
- VisualShaderNodeIntOp <- VisualShaderNode — An integer scalar operator to be used within the visual shader graph.
- VisualShaderNodeIntParameter <- VisualShaderNodeParameter — A visual shader node for shader parameter (uniform) of type [int].
- VisualShaderNodeIs <- VisualShaderNode — A boolean comparison operator to be used within the visual shader graph.
- VisualShaderNodeLinearSceneDepth <- VisualShaderNode — A visual shader node that returns the depth value of the DEPTH_TEXTURE node in a linear space.
- VisualShaderNodeMix <- VisualShaderNode — Linearly interpolates between two values within the visual shader graph.
- VisualShaderNodeMultiplyAdd <- VisualShaderNode — Performs a fused multiply-add operation within the visual shader graph.
- VisualShaderNodeOuterProduct <- VisualShaderNode — Calculates an outer product of two vectors within the visual shader graph.
- VisualShaderNodeOutput <- VisualShaderNode — Represents the output shader parameters within the visual shader graph.
- VisualShaderNodeParameter <- VisualShaderNode — A base type for the parameters within the visual shader graph.
- VisualShaderNodeParameterRef <- VisualShaderNode — A reference to an existing VisualShaderNodeParameter.
- VisualShaderNodeParticleAccelerator <- VisualShaderNode — A visual shader node that accelerates particles.
- VisualShaderNodeParticleBoxEmitter <- VisualShaderNodeParticleEmitter — A visual shader node that makes particles emitted in a box shape.
- VisualShaderNodeParticleConeVelocity <- VisualShaderNode — A visual shader node that makes particles move in a cone shape.
- VisualShaderNodeParticleEmit <- VisualShaderNode — A visual shader node that forces to emit a particle from a sub-emitter.
- VisualShaderNodeParticleEmitter <- VisualShaderNode — A base class for particle emitters.
- VisualShaderNodeParticleMeshEmitter <- VisualShaderNodeParticleEmitter — A visual shader node that makes particles emitted in a shape defined by a Mesh.
- VisualShaderNodeParticleMultiplyByAxisAngle <- VisualShaderNode — A visual shader helper node for multiplying position and rotation of particles.
- VisualShaderNodeParticleOutput <- VisualShaderNodeOutput — Visual shader node that defines output values for particle emitting.
- VisualShaderNodeParticleRandomness <- VisualShaderNode — Visual shader node for randomizing particle values.
- VisualShaderNodeParticleRingEmitter <- VisualShaderNodeParticleEmitter — A visual shader node that makes particles emitted in a ring shape.
- VisualShaderNodeParticleSphereEmitter <- VisualShaderNodeParticleEmitter — A visual shader node that makes particles emitted in a sphere shape.
- VisualShaderNodeProximityFade <- VisualShaderNode — A visual shader node representing proximity fade effect.
- VisualShaderNodeRandomRange <- VisualShaderNode — A visual shader node that generates a pseudo-random scalar.
- VisualShaderNodeRemap <- VisualShaderNode — A visual shader node for remap function.
- VisualShaderNodeReroute <- VisualShaderNode — A node that allows rerouting a connection within the visual shader graph.
- VisualShaderNodeResizableBase <- VisualShaderNode — Base class for resizable nodes in a visual shader graph.
- VisualShaderNodeRotationByAxis <- VisualShaderNode — A visual shader node that modifies the rotation of the object using a rotation matrix.
- VisualShaderNodeSDFRaymarch <- VisualShaderNode — SDF raymarching algorithm to be used within the visual shader graph.
- VisualShaderNodeSDFToScreenUV <- VisualShaderNode — A function to convert an SDF (signed-distance field) to screen UV, to be used within the visual shader graph.
- VisualShaderNodeSample3D <- VisualShaderNode — A base node for nodes which samples 3D textures in the visual shader graph.
- VisualShaderNodeScreenNormalWorldSpace <- VisualShaderNode — A visual shader node that unpacks the screen normal texture in World Space.
- VisualShaderNodeScreenUVToSDF <- VisualShaderNode — A function to convert screen UV to an SDF (signed-distance field), to be used within the visual shader graph.
- VisualShaderNodeSmoothStep <- VisualShaderNode — Calculates a SmoothStep function within the visual shader graph.
- VisualShaderNodeStep <- VisualShaderNode — Calculates a Step function within the visual shader graph.
- VisualShaderNodeSwitch <- VisualShaderNode — A selector function for use within the visual shader graph.
- VisualShaderNodeTexture <- VisualShaderNode — Performs a 2D texture lookup within the visual shader graph.
- VisualShaderNodeTexture2DArray <- VisualShaderNodeSample3D — A 2D texture uniform array to be used within the visual shader graph.
- VisualShaderNodeTexture2DArrayParameter <- VisualShaderNodeTextureParameter — A visual shader node for shader parameter (uniform) of type Texture2DArray.
- VisualShaderNodeTexture2DParameter <- VisualShaderNodeTextureParameter — Provides a 2D texture parameter within the visual shader graph.
- VisualShaderNodeTexture3D <- VisualShaderNodeSample3D — Performs a 3D texture lookup within the visual shader graph.
- VisualShaderNodeTexture3DParameter <- VisualShaderNodeTextureParameter — Provides a 3D texture parameter within the visual shader graph.
- VisualShaderNodeTextureParameter <- VisualShaderNodeParameter — Performs a uniform texture lookup within the visual shader graph.
- VisualShaderNodeTextureParameterTriplanar <- VisualShaderNodeTextureParameter — Performs a uniform texture lookup with triplanar within the visual shader graph.
- VisualShaderNodeTextureSDF <- VisualShaderNode — Performs an SDF (signed-distance field) texture lookup within the visual shader graph.
- VisualShaderNodeTextureSDFNormal <- VisualShaderNode — Performs an SDF (signed-distance field) normal texture lookup within the visual shader graph.
- VisualShaderNodeTransformCompose <- VisualShaderNode — Composes a Transform3D from four Vector3s within the visual shader graph.
- VisualShaderNodeTransformConstant <- VisualShaderNodeConstant — A Transform3D constant for use within the visual shader graph.
- VisualShaderNodeTransformDecompose <- VisualShaderNode — Decomposes a Transform3D into four Vector3s within the visual shader graph.
- VisualShaderNodeTransformFunc <- VisualShaderNode — Computes a Transform3D function within the visual shader graph.
- VisualShaderNodeTransformOp <- VisualShaderNode — A Transform3D operator to be used within the visual shader graph.
- VisualShaderNodeTransformParameter <- VisualShaderNodeParameter — A Transform3D parameter for use within the visual shader graph.
- VisualShaderNodeTransformVecMult <- VisualShaderNode — Multiplies a Transform3D and a Vector3 within the visual shader graph.
- VisualShaderNodeUIntConstant <- VisualShaderNodeConstant — An unsigned scalar integer constant to be used within the visual shader graph.
- VisualShaderNodeUIntFunc <- VisualShaderNode — An unsigned scalar integer function to be used within the visual shader graph.
- VisualShaderNodeUIntOp <- VisualShaderNode — An unsigned integer scalar operator to be used within the visual shader graph.
- VisualShaderNodeUIntParameter <- VisualShaderNodeParameter — A visual shader node for shader parameter (uniform) of type unsigned [int].
- VisualShaderNodeUVFunc <- VisualShaderNode — Contains functions to modify texture coordinates (`uv`) to be used within the visual shader graph.
- VisualShaderNodeUVPolarCoord <- VisualShaderNode — A visual shader node that modifies the texture UV using polar coordinates.
- VisualShaderNodeVarying <- VisualShaderNode — A visual shader node that represents a "varying" shader value.
- VisualShaderNodeVaryingGetter <- VisualShaderNodeVarying — A visual shader node that gets a value of a varying.
- VisualShaderNodeVaryingSetter <- VisualShaderNodeVarying — A visual shader node that sets a value of a varying.
- VisualShaderNodeVec2Constant <- VisualShaderNodeConstant — A Vector2 constant to be used within the visual shader graph.
- VisualShaderNodeVec2Parameter <- VisualShaderNodeParameter — A Vector2 parameter to be used within the visual shader graph.
- VisualShaderNodeVec3Constant <- VisualShaderNodeConstant — A Vector3 constant to be used within the visual shader graph.
- VisualShaderNodeVec3Parameter <- VisualShaderNodeParameter — A Vector3 parameter to be used within the visual shader graph.
- VisualShaderNodeVec4Constant <- VisualShaderNodeConstant — A 4D vector constant to be used within the visual shader graph.
- VisualShaderNodeVec4Parameter <- VisualShaderNodeParameter — A 4D vector parameter to be used within the visual shader graph.
- VisualShaderNodeVectorBase <- VisualShaderNode — A base type for the nodes that perform vector operations within the visual shader graph.
- VisualShaderNodeVectorCompose <- VisualShaderNodeVectorBase — Composes a Vector2, Vector3 or 4D vector (represented as a Quaternion) from scalars within the visual shader graph.
- VisualShaderNodeVectorDecompose <- VisualShaderNodeVectorBase — Decomposes a Vector2, Vector3 or 4D vector (represented as a Quaternion) into scalars within the visual shader graph.
- VisualShaderNodeVectorDistance <- VisualShaderNodeVectorBase — Returns the distance between two points.
- VisualShaderNodeVectorFunc <- VisualShaderNodeVectorBase — A vector function to be used within the visual shader graph.
- VisualShaderNodeVectorLen <- VisualShaderNodeVectorBase — Returns the length of a Vector3 within the visual shader graph.
- VisualShaderNodeVectorOp <- VisualShaderNodeVectorBase — A vector operator to be used within the visual shader graph.
- VisualShaderNodeVectorRefract <- VisualShaderNodeVectorBase — Returns the vector that points in the direction of refraction.
- VisualShaderNodeWorldPositionFromDepth <- VisualShaderNode — A visual shader node that calculates the position of the pixel in world space using the depth texture.
- VoxelGI <- VisualInstance3D — Real-time global illumination (GI) probe.
- VoxelGIData <- Resource — Contains baked voxel global illumination data for use in a VoxelGI node.
- WeakRef <- RefCounted — Holds an Object.
- Window <- Viewport — Base class for all windows, dialogs, and popups.
- WorkerThreadPool <- Object — A singleton that allocates some Threads on startup, used to offload tasks to these threads.
- World2D <- Resource — A resource that holds all components of a 2D world, such as a canvas and a physics space.
- World3D <- Resource — A resource that holds all components of a 3D world, such as a visual scenario and a physics space.
- WorldBoundaryShape2D <- Shape2D — A 2D world boundary (half-plane) shape used for physics collision.
- WorldBoundaryShape3D <- Shape3D — A 3D world boundary (half-space) shape used for physics collision.
- WorldEnvironment <- Node — Default environment properties for the entire scene (post-processing effects, lighting and background settings).
- X509Certificate <- Resource — An X509 certificate (e.
- XMLParser <- RefCounted — Provides a low-level interface for creating parsers for XML files.
- XRAnchor3D <- XRNode3D — An anchor point in AR space.
- XRBodyModifier3D <- SkeletonModifier3D — A node for driving body meshes from XRBodyTracker data.
- XRBodyTracker <- XRPositionalTracker — A tracked body in XR.
- XRCamera3D <- Camera3D — A camera node which automatically positions itself based on XR tracking data.
- XRController3D <- XRNode3D — A 3D node representing a spatially-tracked controller.
- XRControllerTracker <- XRPositionalTracker — A tracked controller.
- XRFaceModifier3D <- Node3D — A node for driving standard face meshes from XRFaceTracker weights.
- XRFaceTracker <- XRTracker — A tracked face.
- XRHandModifier3D <- SkeletonModifier3D — A node for driving hand meshes from XRHandTracker data.
- XRHandTracker <- XRPositionalTracker — A tracked hand in XR.
- XRInterface <- RefCounted — Base class for an XR interface implementation.
- XRInterfaceExtension <- XRInterface — Base class for XR interface extensions (plugins).
- XRNode3D <- Node3D — A 3D node that has its position automatically updated by the XRServer.
- XROrigin3D <- Node3D — The origin point in AR/VR.
- XRPose <- RefCounted — This object contains all data related to a pose on a tracked object.
- XRPositionalTracker <- XRTracker — A tracked object.
- XRTracker <- RefCounted — A tracked object.
- XRVRS <- Object — Helper class for XR interfaces that generates VRS images.
- bool — A built-in boolean type.
- float — A built-in type for floating-point numbers.
- int — A built-in type for integers.
