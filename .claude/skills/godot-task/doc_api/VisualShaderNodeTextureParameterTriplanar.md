## VisualShaderNodeTextureParameterTriplanar <- VisualShaderNodeTextureParameter

Performs a lookup operation on the texture provided as a uniform for the shader, with support for triplanar mapping.

