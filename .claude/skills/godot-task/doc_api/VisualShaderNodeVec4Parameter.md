## VisualShaderNodeVec4Parameter <- VisualShaderNodeParameter

Translated to `uniform vec4` in the shader language.

**Props:**
- default_value: Vector4 = Vector4(0, 0, 0, 0)
- default_value_enabled: bool = false

