## VisualShaderNodeVec3Parameter <- VisualShaderNodeParameter

Translated to `uniform vec3` in the shader language.

**Props:**
- default_value: Vector3 = Vector3(0, 0, 0)
- default_value_enabled: bool = false

