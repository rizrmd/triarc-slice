## VisualShaderNodeCubemapParameter <- VisualShaderNodeTextureParameter

Translated to `uniform samplerCube` in the shader language. The output value can be used as port for VisualShaderNodeCubemap.

