## VisualShaderNodeVec2Parameter <- VisualShaderNodeParameter

Translated to `uniform vec2` in the shader language.

**Props:**
- default_value: Vector2 = Vector2(0, 0)
- default_value_enabled: bool = false

